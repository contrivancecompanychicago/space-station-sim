let state;
describe('Game/Action/Proposer', () => {
  describe('Grid', () => {
    beforeEach(() => {
      state = {UI:{mode:'GRID',grid:'FLOOR'}};
    });
    it('should return a grid object');
  });
});
